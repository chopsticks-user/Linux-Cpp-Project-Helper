	*  Status: Incomplete
	1. Environment: .zshrc
    	    1.1: Add the following into ~/.zshrc if haven't done already:
    	    	 #<---------------------------------------------------------------------->
    	    	 test -f ~/.zsh_custom && .~/.zsh_custom
    	    	 #<---------------------------------------------------------------------->
    
	2. Compiler: g++
	3. CMake: available
